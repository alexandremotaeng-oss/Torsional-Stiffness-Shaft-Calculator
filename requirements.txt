# Dependências do projeto Torsional Stiffness Shaft
#
# Biblioteca padrão (já incluídas no Python – não precisam ser instaladas):
#   tkinter, math, copy, os, base64, webbrowser, datetime, json
#
# Dependências de terceiros:

# Geração de relatório HTML via templates
Jinja2>=3.1

# Exportação de geometria 3D para formato STEP (.stp)
cadquery>=2.4
